#standardSQL
SELECT
  "andre.cardoso@risevision.com, fake-email-address-1@example.com, fake-email-address-2@example.com" AS monitoringEmails,
  display_id displayId
FROM
  `client-side-events.Player_Data.configuration`
WHERE
  display_id = '9G79NYAHBEMA'
GROUP BY
  monitoringEmails,
  displayId
